server:
  port: 10006

spring:
  datasource:
    url: jdbc:mariadb://studydb.choiiq8m684g.ap-northeast-2.rds.amazonaws.com:3306/studydb
    username: studyuser
    password: 12200000
    driver-class-name: org.mariadb.jdbc.Driver

    hikari:
      maximum-pool-size: 2
      minimum-idle: 1
      idle-timeout: 30000
      pool-name: system-service-hikari

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: "MySuperSecureSecretKey_ForStudyLinker_App_2025_AAAABBBBCCCCDDDDEEEEFFFFGGGGHHHHIIIIJJJJ"
  expiration: 3600000

aws:
  region: ap-northeast-2
  rds-instance-id: studydb-instance
  backup:
    enabled: false   # 🔥 로컬에서만 false 로 두고, 나중에 운영은 true

management:
  endpoints:
    web:
      exposure:
        include: health,info